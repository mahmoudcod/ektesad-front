export const { GET_ABOUT } = require('./about');

export const { GET_FORMATS } = require('./format');

export const { GET_POSTS } = require('./post');

export const { GET_FIGURES, GET_FIGURE } = require('./figure');

export const { GET_SETTINGS } = require('./setting');

export const { GET_USERS } = require('./users');

export const { GET_TAGS } = require('./tag');
